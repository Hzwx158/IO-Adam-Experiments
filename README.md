# IO-Adam-ViT experiments

## Usage

Download requirements by: 
```shell
pip install -r requirements.txt
```

Run experiments by:

```shell
python test_train.py \
    --project $WANDB_PROJECT \
    --config "adamw" \
    --name adamw \
    --device cuda:0
```

